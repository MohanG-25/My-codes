
class Solution {
    public boolean checkIfPangram(String sentence) {
        HashSet<Character> l=new HashSet<Character>();
        for(int i=0;i<sentence.length();i++){
            l.add(sentence.charAt(i));
        }
        if(l.size()==26){
            return true;
        }
        return false;
    }
}
